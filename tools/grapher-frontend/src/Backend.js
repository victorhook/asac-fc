let config = {
    blocks: [
        {
            name: "Gyro",
            params: [
                "raw_gyro_x",
                "raw_gyro_y",
                "raw_gyro_z",
            ]
        }
    ]
};


class Backend {
    /*
        Saves the params state for the given 
    */
    static save(params) {

    }
}